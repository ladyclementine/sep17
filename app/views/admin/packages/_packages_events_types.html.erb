<%= form_for [:admin, @package, @package_event_type] do |f| %>
  <% if @package_event_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@package_event_type.errors.count, "error") %> prohibited this package_event_type from being saved:</h2>
      <ul>
      <% @package_event_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="form-group row">
      <div class="col-sm-6">
        <%= f.label :event_type %>
        <%= f.select :event_type_id, options_for_select(event_types_to_array), {}, class: 'form-control' %>
      </div>
      <div class="col-sm-6">
        <%= f.label :limit %>
        <%= f.number_field :limit, class: 'form-control' %>
      </div>
    </div>
    <div class="form-group text-right m-b-0">
      <%= button_tag "Adicionar tipo de programação", type:'submit', class: 'btn btn-purple waves-effect waves-light' %>
    </div>
<% end %>