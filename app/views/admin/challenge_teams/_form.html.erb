<%= form_for [:admin, @package] do |f| %>
    <% if @package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@package.errors.count, "error") %> prohibited this package from being saved:</h2>

      <ul>
      <% @package.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :description %>
        <%= f.text_area :description, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :courses %>
        <%= f.number_field :courses, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :lectures %>
        <%= f.number_field :lectures, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :visits %>
        <%= f.number_field :visits, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :limit %>
        <%= f.number_field :limit, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :price %>
        <%= f.text_field :price, class: 'form-control' %>
      </div>  
      <div class="form-group text-right m-b-0">
        <%= button_tag "Save package", type:'submit', class:'btn btn-purple waves-effect waves-light' %>
      </div>
<% end %>